spring:
  datasource:
    url: jdbc:postgresql://localhost:1111/course_test
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    allow-circular-references: true

server:
  port: 1111


